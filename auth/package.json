{
  "name": "better-auth",
  "version": "1.0.0",
  "description": "Better Auth service for Port-au-Next",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "migrate": "npx @better-auth/cli migrate",
    "generate": "npx @better-auth/cli generate"
  },
  "dependencies": {
    "@better-auth/cli": "^1.2.4",
    "better-auth": "^1.2.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.5",
    "@types/pg": "^8.10.9",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
